import { createClient } from '@insforge/sdk';

// Initialize InsForge client with provided credentials
export const insforge = createClient({
  baseUrl: 'https://46u6khrz.us-east.insforge.app',
  anonKey: 'ik_a98b0c8031e2accda4559e6ce2641b49'
});
